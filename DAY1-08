# Data for mobile phones sold and money earned
mobiles_sold <- c(4, 1, 5, 7, 10, 2, 50, 25, 90, 36)
money_earned <- c(12, 5, 13, 19, 31, 7, 153, 72, 275, 110)

# Create the scatter plot
plot(mobiles_sold, money_earned, 
     xlab = "Number of Mobile Phones Sold", 
     ylab = "Money Earned", 
     main = "Scatter Plot of Mobile Phones Sold vs. Money Earned")

# Add points with different colors and shapes (optional)
points(mobiles_sold, money_earned, col = "blue", pch = 16)  # Blue circles

# Customize the plot further (optional)
# You can add axis limits, gridlines, legends, etc. using functions like
# abline(), grid(), and legend()
